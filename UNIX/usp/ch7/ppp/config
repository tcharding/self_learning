# Pipeline Pre-Proces Config file
#
# format: 'a string'
# where 'a' is transformed into string
# string is everything on line after character + whitespace
#
# eg
# a this is all part of string
# b this [{+}[{=} }[{  is also fine
#  c   whitespace at start of line and after 'c' permitted
a |simple|
b |little harder|
