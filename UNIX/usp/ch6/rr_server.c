#include "tch.h"
#include "request_reply.h"

#define RQ_PERMS (S_IRUSR | S_IWUSR | S_IWGRP | S_IWOTH)

/* request/reply server */
int main(void)
{
	
	/* char *msg; */
	/* char *rq = RQ_FIFO; */
	/* char *rq_buf[PIPE_BUF]; */
	
	/* if ((mkfifo(rq, RQ_PERMS) == -1) && (errno != EEXIST)) */
	/* 	err_sys("mkfifo error"); */

	/* rqfd = Open(rq, O_RDONLY); */
	/* Read(rq_fd, rq_buf, PIPE_BUF); /\* Can this read more than one request? 
	 */
	
	
	return 0;
}
